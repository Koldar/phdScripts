\newglossaryentry{eventstream}
{
    name={eventstream},
    description={Is a server-push technology enabling a client to receive automatic updates from a server via \acr{HTTP} connection. The Server-Sent Events EventSource API is standardized as part of HTML5 by the W3C\cite{eventstream}.}
}
